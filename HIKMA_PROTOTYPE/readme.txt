How to Use This on a New Machine (e.g., Raspberry Pi,or a new laptop)
When you are ready to move this brain to the actual robot hardware (Raspberry Pi), you simply:

Copy this entire folder to the Pi.

Open the terminal inside the folder.

Run this single command to install everything:

DOS
pip install -r requirements.txt

Run the robot:

DOS
python main.py